# Install Download Dependencies

To enable the PDF download functionality, run:

```bash
npm install html2canvas jspdf
```

This will install:
- `html2canvas`: Captures DOM elements as images
- `jspdf`: Generates PDF files from images

The download feature is now available on:
- Dashboard page
- Transactions page  
- Analytics page

Users can download complete reports including all charts and data as PDF files.